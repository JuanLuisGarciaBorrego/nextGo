import React from 'react';
import {APP_NAME} from "../constants/app";

export default function Logo({size = 6}) {
    return(
        <></>
        // <img className={`h-${size} w-${size} rounded-md`}  src="/static/logo.jpg" alt={APP_NAME}/>
    )
}
